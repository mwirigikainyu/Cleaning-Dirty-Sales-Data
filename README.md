# Cleaning-Dirty-Sales-Data
Cleaning and preprocessing dirty sales data in preparation for data analysis using pandas


To create a conda virtual environment use:

`conda create --name <env> --file requirements.txt`
